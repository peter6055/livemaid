# LiveMaid Features & Implementation Truths

This document serves as the source of truth for the implemented features and architectural decisions in LiveMaid.
**ALL AGENTS MUST READ THIS DOCUMENT before making changes to ensure existing features are not accidentally broken.**

## 1. Diagram Types & Two-Way Sync
- **Supported Two-Way Sync**: `flowchart` (and `graph`) and `sequenceDiagram`. These diagrams allow visual editing (e.g., double-clicking text, adding nodes via buttons) which automatically updates the underlying Mermaid code.
- **Visual-Only (One-Way Sync)**: All other Mermaid diagram types. These render correctly but are read-only on the canvas. Users must edit the code directly to change them.
- **Implementation**: The `determineDiagramType` function in `src/lib/diagrams/utils.ts` determines the type. Features like the Quick Add Node button rely on this type to know what code to append.

## 2. Pan & Zoom Engine (`react-zoom-pan-pinch`)
- **Infinite Canvas (Mouse Dragging)**: We use `react-zoom-pan-pinch`. We explicitly set `limitToBounds={false}`. This is **CRITICAL** because without it, the library locks panning when the diagram fits entirely within the view, breaking the ability to drag the canvas with the mouse.
- **Wheel Config**: `wheel={{ wheelDisabled: true, step: 0.05 }}` is used on the main editor canvas to prevent accidental wheel-zoom while editing.
- **Trackpad Interaction**: Trackpad panning remains enabled (`trackPadPanning={{ disabled: false }}`), and zoom is performed through the zoom controls and pinch gestures.
- **Trackpad Swipe Navigation Guard**: The app globally applies `overscroll-behavior-x: none` on `html` and `body` to block browser-level horizontal swipe navigation (back/forward page changes) while using trackpad gestures.
- **Zoom Constraints**: We set `minScale={0.5}` and `maxScale={50}`. The `minScale` limit prevents diagrams from zooming out into tiny, unreadable, or lost shapes.
- **Interaction Locks**: `panning={{ disabled: isInlineEditing || dragState?.isDragging }}` is set to prevent the canvas from moving while the user is typing in the inline editor or dragging sequence elements.

## 3. Interactive Node Selection & Editing
- **Node Resolution (`getClickedNode`)**: When a user clicks the canvas, we traverse up the DOM tree from the `e.target` to find a container with the `.node` or `.cluster` class. 
- **ID Normalization**: Mermaid injects dynamic prefixes (e.g. `flowchart-mermaid-svg-1234-`). The `getClickedNode` function cleans this raw SVG ID into a clean ID (e.g., `A`) that matches the actual Mermaid code. Both `rawSvgId` and `cleanId` are returned.
- **Selection Box**: We calculate the bounding box of the `.label`, `foreignObject`, or `text` inside the node and apply the current zoom `scale` to render a perfectly aligned selection outline.
- **Layout Alignment Observers**:
  - **compilation tracking**: Since Mermaid compilation is asynchronous, we hook selection recalculation directly to changes in both `code` and `svgContent` (the compiled output in the DOM) using a 50ms layout timeout.
  - **ResizeObserver**: To ensure that the selection box and text edit overlays remain glued to the target node during screen resizing or split-panel dragging, we mount a `ResizeObserver` observing both `containerRef.current` and `.mermaid-container`.
- **Dynamic Style Highlights**: The floating style bar queries the raw Mermaid styling code using a static regex scraper (`getStyleFromCode`) alongside native `window.getComputedStyle(element)` queries to dynamically toggle active outlines on preset background/border/text colors and bold/italic font states.
- **Node Styling Reset**: Clicking the Reset Style button in the `NodeManipulationToolbar` completely purges all `style <nodeId>` lines from the raw Mermaid source. This completely reverts custom background color, border color, text color, font weight, and italic traits, immediately forcing the node to adhere back to the active diagram theme.
- **Mermaid Style Color Sanitization Guard**: Before persisting editor code, style lines are sanitized so `rgb(...)` / `rgba(...)` color tokens are converted to Mermaid-safe `#RRGGBB` values. This prevents parse failures caused by comma-delimited CSS colors inside `style <id> ...` declarations and auto-heals previously saved invalid style lines.
- **Physical Size Zoom Locking**: The floating styling bar (`NodeManipulationToolbar`), inline formatting toolbar, inline textarea overlay, and Quick Add (+ Button) employ real-time, zero-lag DOM-level scale-locking (via attributes `data-scale-lock` and `data-base-transform`) inside react-zoom-pan-pinch's `onTransformed` and `onInit` callbacks. This completely bypasses React render delay and Safari's CSS variable transition limitations, guaranteeing a perfectly locked physical sizing.
- **Clamped Scale-Lock (`data-scale-lock-max1`) — constant when zooming IN, proportional when zooming OUT**: A variant of the above for on-canvas affordances that should NOT keep a fixed screen size at extreme zoom-OUT. `updateScaleLockedElements` applies `scale(min(1, 1/zoom))` to `[data-scale-lock-max1]` elements (vs the plain `scale(1/zoom)` for `[data-scale-lock]`). Effect: when zoomed IN (zoom > 1) the element counter-scales DOWN to a constant on-screen size (never balloons); when zoomed OUT (zoom < 1) the clamp pins the factor at `1×` so the element SHRINKS WITH the diagram instead of staying a fixed screen size that dwarfs a densely-packed, zoomed-out diagram. Used by the message endpoint drag bars (see §15) — at min zoom (0.5×) a plain scale-locked 44px bar spanned ~8 message rows and dominated; with the clamp it renders ~22px and fits the message band. Any `[data-scale-lock-max1]` element must ALSO be listed in the two `hitFloatingUi` guards (the document-capture mousedown guard and the canvas `onDoubleClick` guard) so its presses are treated as floating UI and never leak into canvas hit-testing/deselection.
- **Decoupled Selection Outlines**: The selection box container does NOT receive scale transforms (leaving its width, height, left, and top coordinates perfectly matching the node in SVG coordinates), while its border and shadow thicknesses are separately scale-locked using `data-scale-lock-border` and `data-scale-lock-shadow`. This prevents selection box distortion or misalignment at any zoom depth.
- **Auto-Deselection on Zoom**: To ensure a clean, distraction-free workspace and prevent layout scaling clutter during navigation, zooming in, zooming out, resetting transform, or starting a trackpad pinch-zoom gesture automatically deselects any currently active selection. This instantly closes the `NodeManipulationToolbar` and floating controls, immediately focusing interaction back on the canvas.
- **Flowchart Node Hover Highlight**: On `mousemove`, when the canvas is in `flowchart`/`graph` mode, `useCanvasInteraction` detects the hovered `.node` SVG element via `e.target.closest('.node')`, measures its viewport bounding rect, and converts to canvas-space coordinates using `(rect.* - containerRect.*) / scale`. This is stored as `hoveredFlowchartNodeBox` (state in the hook, propagated through `EditorCanvas` props). `EditorCanvas` renders a thin `border-indigo-400` overlay div (`pointer-events: none`, `z-[19]`, 1.5px border scale-locked) around the hovered node. The highlight is suppressed during inline editing, active connection drawing, and selection-box creation.
- **Rename Button in Node Toolbar**: `NodeManipulationToolbar` exposes an optional `onEditLabel?: (e: React.MouseEvent) => void` prop. When provided, a `Pencil` icon button is rendered as the first item in the toolbar (before color pickers). Clicking it calls `handleEditClick` in `EditorCanvas`, triggering the same inline text editor flow as a physical double-click. This gives users a discoverable affordance for renaming a node without requiring a precise double-click gesture.
- **Unified Interaction State Machine (IDLE / SELECT_MODE / EDIT_MODE)**: Canvas interaction is centralized in `useCanvasInteraction` with strict transitions and a capture fallback in `EditorCanvas`:
  - **IDLE → SELECT_MODE**: single click on node/edge selects it and shows the floating toolbar.
  - **Double-click edit entry (robust path)**: double-click is detected in `handleSvgClick` via `e.detail >= 2` when the clicked element matches `selectedNodeIdRef.current`; this calls `handleEditClick` and enters EDIT_MODE.
  - **Document capture fallback for single-click reliability**: `EditorCanvas` registers a `document.addEventListener('mousedown', ..., true)` listener and routes a synthetic event (including `detail`, `clientX`, `clientY`) into `handleSvgClick`. This is required because Mermaid SVG + `foreignObject` content can bypass normal React bubbling on single-click in some paths.
  - **Cross-element single-click while editing**: clicking node B while editing node A commits A (`commitEditRef.current?.()`), exits EDIT_MODE, and transitions directly to SELECT_MODE on B.
  - **Cross-element double-click while editing**: commit current edit, transition selection, then allow immediate re-entry to EDIT_MODE on target via click-detail path.
  - **Outside click → IDLE**: clicking empty canvas clears selection and exits overlay state.
  - **`commitEditRef`**: a `useRef` slot in `useCanvasInteraction` that `LiveMaidEditor` fills with `handleEditSubmit`, enabling commit-before-transition without stale closure issues.
  - **No-op commit guard**: `handleEditSubmit` exits early if new text does not change Mermaid code, preventing unnecessary recompiles and layout drift.
  - **Debug logging (opt-in only)**: click diagnostics are behind `window.__LM_DEBUG_CLICKS = true` or `localStorage['livemaid:debug-clicks']='1'`; default runtime is quiet.

## 4. Inline Text Editing (The Overlay)
- **Overlay Positioning**: When a user double-clicks a node, an absolute-positioned `<textarea>` is rendered on top of the diagram. It calculates its exact position and size based on the SVG element's bounding box.
- **Hiding Original Text**: To avoid double text (seeing the SVG text behind the textarea), we inject a dynamic `<style>` tag that sets `opacity: 0 !important` on the specific `#${selectedSvgId} .label, text, foreignObject` while editing is active.
- **Event Propagation (CRITICAL)**: All overlay UI elements (toolbar, textarea) MUST have `onMouseDown={(e) => e.stopPropagation()}` and `onClick={(e) => e.stopPropagation()}`. If this is missed, clicking the overlay will trigger the canvas pan/zoom handlers underneath and steal focus or cause jitter.
- **High-Contrast Text Highlight Selection**: To guarantee that selected/highlighted text remains visible in all OS/browser color themes (preventing dark-mode overrides from rendering selected text invisible), the overlay input explicitly enforces high-specificity indigo selection styles: `selection:bg-indigo-600 selection:text-white`.
- **Spacious Width Boundaries**: To prevent premature wrapping and make long typed words fully legible, the overlay textarea calculates its unscaled target width as `Math.min(Math.max(textBox.width + 200, 350), 700)`.
- **Drift-Free Viewport Coordinate Bounds**: Hover elements, overlays, and context menus (like `shapePicker`) that render in the static viewport space must calculate coordinates by subtracting `.getBoundingClientRect()` of the closest static container (`.relative.overflow-hidden`) rather than the zoomed/panned inner canvas (`containerRef.current`). This guarantees 1:1 screen-to-cursor matching with absolute precision under any zoom or pan level.

## 5. Quick Add Node (+ Button)
- **Visibility**: Appears below the selection box when a node is selected (and inline editing is not active).
- **Code Generation (`handleAddNodeFromSelected`)**: 
  - For flowcharts: Appends `\n {selectedNodeId} --> NewNode{i}[New Node]`. It auto-increments the `i` to avoid ID collisions.
  - For sequence diagrams: Appends `\n {selectedNodeId}->>NewActor: new msg`.
- **Sequence UX Rule**: In sequence diagrams, the selected-node quick-add `+` control is hidden to prevent click-to-add creation from the selection box. Sequence creation remains available through lifeline anchor `+` handles via drag-to-connect interactions.

## 6. Theme Management
- We manually inject or update the Mermaid configuration block (`--- \n config: \n theme: 'dark' \n ---`) at the top of the raw Mermaid code string to control themes. This ensures the theme is persisted directly in the diagram code itself.

## 7. Modular Architecture
- The editor logic is split into several modular components in `src/components/editor/`: `EditorHeader`, `EditorCodePanel`, `EditorCanvas`, `NodeManipulationToolbar`, and `InlineTextEditor`.
- State logic is handled by `useEditorState` and `useCanvasInteraction` custom hooks.
- Extracted constants and utilities live in `src/lib/diagrams/`.

## 8. Dashboard & Card Preview Skeletons
- **High-Fidelity Dashboard Skeleton Loaders**: Replacing standard loading spinners, the dashboard uses a premium animated CSS grid of 6 skeleton cards that match `DiagramCard` proportions and layouts.
- **Asynchronous Card Preview Skeletons**: Diagram cards employ an `isCompiling` state during asynchronous Mermaid SVG compilation. While compiling, a high-fidelity animated flowchart preview skeleton—complete with rounded start nodes, a rotated decision diamond, an end rectangle, and connectors/arrows—is rendered to represent the loading diagram.
- **White Background Previews**: All diagram card previews and skeleton loaders utilize high-contrast `bg-white` canvas containers to ensure high legibility and clear contrast in both light and dark modes.

## 9. Flowchart Edge/Connector Styling & Label Editing
- **Interactive Edge Selection**: Users can click directly on a flowchart link (edge path) or its text label. The editor identifies the adjacent `.edgeLabel` node, snaps the coordinates to the label, and assigns it a queryable dynamic ID (`edge-label-${cleanId}`).
- **Strict Parallel Edge Isolation (Hover & Selection)**: Mermaid v11+ automatically injects a unique, canonical `data-id` attribute containing the exact path ID (e.g. `L_src_dst_index`) on the `<g class="label">` container inside *every* `.edgeLabel` container (both labeled and unlabeled). We map hover highlight and click selection strictly using these canonical IDs instead of array index fallbacks (`edgeLabels.indexOf`). This completely eliminates index-drift, ensuring that hover isolation and selection are strictly isolated to individual parallel edge occurrences without parallel leakage or mapping drift.
- **Unlabeled Edge Bounding Box Snapping**: If an edge has no label, its `.edgeLabel` container exists but contains empty text and has zero width/height. To prevent the selection overlay from collapsing or flying to `(0, 0)`, coordinates for unlabeled edges are measured directly from the visual `<path.flowchart-link>` bounding box rather than snapping to the empty label container.
- **Edge Manipulation Toolbar**: When an edge is selected, a dedicated scale-locked floating toolbar appears on the canvas. It provides quick controls for:
  - **Arrow Style**: Configures the connector arrowhead (plain, arrow, double arrow, cross, double cross, circle, double circle).
  - **Stroke Style**: Configures the link dash pattern and weight (solid, dashed, thick).
  - **Bidirectional Style Coercion for Thick & Unsupported Connectors**: Mermaid does not natively support thick variants for cross (`--x`) or circle (`--o`) arrowheads. To prevent compilation/syntax errors:
    - **Stroke to Thick**: If the user selects "Thick Line" from the stroke dropdown while an unsupported arrowhead style (Cross, Double Cross, Circle, Double Circle) is active, the arrow style is automatically coerced to its standard thick-supported counterpart (`arrow` or `double_arrow`), allowing the stroke change to succeed.
    - **Arrow Style to Cross/Circle**: If the user selects an unsupported arrowhead style (Cross, Double Cross, Circle, Double Circle) while the line is currently a "Thick Line", the stroke style is automatically coerced to "Solid Line", allowing the arrowhead style change to succeed.
  - **Line Color**: Toggles the active outline on preset stroke colors.
  - **Zap/Flow Animation**: Configures real-time animated flow effects across the path using Mermaid's native v11+ property-based edge animation syntax. When enabled, a deterministic edge ID (e.g. `e_A_B_0@`) is prepended to the connector on the targeted edge line, and a corresponding property declaration `e_A_B_0@{ animate: true }` is cleanly appended to the end of the diagram. When animation is toggled off and the edge uses our deterministic ID pattern, both the property declaration and the prepended ID prefix are automatically removed, restoring the clean diagram syntax. Manual/custom user-defined edge IDs are fully preserved during animation toggling.
    - **Inline `@{...}` shape-source split (CRITICAL)**: When the edge's SOURCE node carries inline shape metadata on the same line (e.g. `A2@{ shape: flip-tri, label: "x" } ==>B`), the edge ID can NOT be placed after the `}` — `A2@{...} e_A2_B_0@==>B` is invalid Mermaid (`Parse error: Expecting 'AMP', got 'LINK_ID'`) and the diagram fails to render, so the animation silently never appears. `updateLinkAnimation` detects an inline `@{...}` shape on the source and SPLITS it: the shape stays on its own node-definition line and the edge ID is placed after the bare source id — `A2@{...}` + newline + `A2 e_A2_B_0@==>B`. Duplicate node-def lines are harmless (Mermaid merges them). Bracket-label sources like `A["x"] e_A_B_0@-->B` are unaffected; only the `@{...}` shape syntax requires the split.
  - **Edit Label Text**: Centers the glassmorphic `<textarea>` overlay directly over the edge label for inline editing.
  - **Delete Edge**: Deletes the connection from the raw Mermaid source. To prevent accidental node deletion, the source and target nodes are preserved as standalone parentless/orphan nodes in the flowchart if they are not defined or referenced elsewhere in the diagram code.
- **Global Edge Curve Config**: Located in the main flowchart top bar toolbar, this allows toggling the global flowchart interpolation/routing curve style (orthogonal step, curve basis, linear straight) for all links in the flowchart, persisting directly inside the diagram config frontmatter block.
- **Two-Way Label Sync**: Double-clicking an edge label, or clicking the pencil button, triggers the dynamic `<InlineTextEditor>` at the exact coordinates of the label. Submitting the text (by pressing Enter or blurring) serializes the edge label back to the Mermaid code in the standard format (e.g. `A -->|New Label| B`) and instantly re-renders the diagram on the canvas.
- **Link Style Preservation & RebuildLinkStyles Bypass**: Styling updates like Line Color and Zap/Flow Animation append `linkStyle` rules to the end of the Mermaid code. Because style modifications do not alter the link structure (no lines added or removed), `rebuildLinkStyles` is bypassed for these operations, passing `updatedCode` directly to `handleCodeChange`. This prevents custom style rules from being lost during compilation rendering.
- **Centralized Connector Matching Pattern**: Standardized and ordered all Mermaid link connectors from longest/most-specific to shortest/least-specific in `CONNECTOR_PATTERN` in `src/lib/diagrams/utils.ts`. This resolves regex prefix conflicts (where shorter subset connectors like `-->` falsely matched inside longer superset connectors like `<-->` or `x--x`). Furthermore, to handle word boundary (`\b`) transitions seamlessly on connectors ending in word characters (such as `--x` or `--o` directly connected to a destination node), we use an elegant lookbehind pattern `(?:\\b|(?<=[xoXO]))${dst}\\b`. This ensures robust arrow type editing, stroke styling, label updating, and styled edge deletion without corrupting the Mermaid syntax.
- **Preset Color Selection Cleanups**: Removed the redundant `'White'` and `'Black'` presets from `PRESET_COLORS` in `src/lib/diagrams/constants.ts` to provide a premium, tailored preset palette for flowchart elements.

## 10. Version History & Rollback
- **Snapshot Source of Truth**: Each diagram document persists a `versionHistory` array in its local JSON file. The editor records the previously saved Mermaid code whenever a real code change is committed through the save path.
- **User Metadata**: History entries can be renamed inline and starred as pinned favorites. These fields live on the same version record and are persisted back through the diagram update route.
- **No Per-Entry Code Preview**: Snapshot rows show the editable name, timestamp, star, and Preview/Rollback actions only. The raw-code `<pre>` snippet that older versions rendered per entry has been removed to keep the list compact; users inspect a version via the read-only Preview canvas instead.
- **Rollback Behavior**: Restoring a version replaces the current code with the selected snapshot and then saves it through the same document update path, which preserves the rollback as a new history entry.
- **Read-Only Preview Workspace**: Opening history now creates a split workspace where the right panel lists snapshots and the left side renders the selected snapshot diagram in a read-only preview canvas. The preview supports pan/zoom/reset controls and is visually marked as non-editable.
- **Preview Interaction Parity**: The history preview canvas mirrors the main canvas interaction model for navigation (`wheelDisabled: true`, `trackPadPanning: enabled`, `limitToBounds: false`) so trackpad panning behaves consistently across both views.
- **Canvas Background Policy**: Diagram render surfaces stay white in both light and dark themes (main editor canvas and history preview canvas) to preserve Mermaid readability and contrast.
- **Pop-out Backdrop Policy**: Pop-out surfaces (dialogs and overlays) use a stronger dimming backdrop in dark mode to preserve depth separation and visual focus (`dark:bg-black/70`).
- **Retention**: Version history is capped at the most recent 100 snapshots so local JSON documents remain predictable while still preserving long-running edit history.

## 11. Testing & Browser Interaction Best Practices
- **Temporary Test Diagrams**: To prevent binding failures, corrupted workspace states, or broken diagrams during interactive browser testing, you MUST always create a new, temporary diagram/flowchart at the start of your test session.
- **Cleanup**: Once your browser testing is completed and verified, you MUST delete or purge the temporary diagram/flowchart to restore the workspace to its clean, original state.
- **Human-Reaction Simulation Protocol (Click/Double-Click Bugs)**:
  - Simulate realistic user pacing, not only synthetic instantaneous sequences: single-click select, then immediate second-node click, then double-click transitions.
  - Validate both behavior and signal: confirm visual state (selection box / inline editor) and diagnostic logs (`[canvas-click]`) for each step.
  - Always compare two paths: clicking label text (`foreignObject`/HTML) vs clicking SVG geometry (`rect`/`path`) because they can dispatch differently.
  - Include negative controls: empty-canvas click and toolbar click to ensure guards (`data-scale-lock`, `data-inline-toolbar`) still suppress unintended transitions.
  - Treat browser dialogs (`beforeunload`) as part of test flow; they can interrupt automation and create false negatives if not resumed.
  - Keep one reproducible script for "human-like" regression: select A, double-click A to edit, immediately double-click B, verify B editor opens and A commit is preserved.

## 12. Global Keyboard Shortcuts
- **Keyboard-Driven Canvas Deletion**: When a diagram node or flowchart edge/line is selected, users can press `Backspace` or `Delete` on their keyboard to instantly delete it from the diagram canvas and Mermaid code.
- **Form/Input and Code Isolation**: The keydown event listener is completely disabled when any input, textarea, contenteditable container, or the Monaco Editor has focus. This prevents elements from being accidentally deleted while typing comments or updating diagram text code.
- **Canvas Focus Blur**: To ensure that selecting canvas elements (which are SVG elements and not focusable by default) immediately enables deletion shortcuts without being blocked by active Monaco editor focus, clicking any canvas element or empty space (inside `handleSvgClick`) programmatically blurs the active text input or editor via `document.activeElement.blur()`. This shifts browser focus away from Monaco/inputs to the document body, activating the deletion listeners safely.

## 13. Exit Confirmation
- **Leave Editor Notice Dialog**: Internal navigation away from the current editor route uses a confirmation dialog before routing, requiring explicit user confirmation (`Leave Editor`) or cancellation (`Stay`).
- **Browser Exit Prompt**: Refreshing or closing the tab while in the editor triggers the browser-native `beforeunload` confirmation prompt.
- **Browser Back Button Guard**: Pressing the browser back button inside the editor is intercepted and routed through the same leave-editor confirmation dialog before navigation is allowed.

## 14. Dashboard Lazy Loading
- **Pagination Strategy**: The dashboard implements progressive loading of diagram cards using IntersectionObserver API. Initially, 6 diagrams are displayed, with additional batches of 6 loaded as the user scrolls down.
- **State Management**: 
  - `displayCount` state tracks the current number of diagrams to display (starts at 6, increments by 6).
  - `sentinelRef` creates a sentinel div at the bottom of the grid that triggers loading when visible.
- **Computed Values**: 
  - `filteredDiagrams` uses `useMemo` to filter all diagrams by search query (only recalculates when diagrams or searchQuery changes).
  - `displayedDiagrams` uses `useMemo` to slice filtered diagrams to show only `displayCount` items (optimizes rendering performance).
- **Intersection Observer**: Watches the sentinel div with `threshold: 0.1`. When the sentinel becomes visible and more diagrams exist, `displayCount` is incremented by 6 (capped at total available).
- **Visual Feedback**: A spinning `Loader2` icon displays at the sentinel position when more diagrams are available, providing clear indication that additional content exists below.
- **Search Integration**: When search query changes, filtered results are recalculated and `displayCount` is effectively reset (since new filtered results use the existing displayCount slice). This ensures search works seamlessly with lazy loading.
- **Performance Benefit**: Lazy loading reduces initial page render time and DOM complexity by only rendering visible cards in the viewport, significantly improving dashboard responsiveness with large diagram libraries.

## 15. Sequence Diagram Visual Interactions
- **Double-Click Rename**: Double-clicking a sequence participant (actor) box opens the `InlineTextEditor` overlay positioned at the actor's header box. On submit, `handleEditSubmit` updates the `as {alias}` portion of the participant declaration in the Mermaid code.
- **Logic-Block / Section Label Rename (double-click)**: Double-clicking a logic block's label box renames it inline. The opener label (e.g. the `Retry` in `loop Retry`, rendered by Mermaid as a `.loopText` `[Retry]` box) and section-divider labels (`else`/`and`/`option`, rendered as `.sectionTitle` boxes) are both editable. On submit only the label PORTION of the source line is rewritten (`handleEditSubmit` `SEQ_BLK_` branch: `/^(\s*)(loop|alt|opt|par|critical|break|else|and|option)\b[ \t]*(.*)$/` → keep indent + keyword, replace the rest; an empty new label collapses to the bare keyword). Implementation details:
  - **DOM→source mapping is by Y-sort, NOT DOM order (CRITICAL)**: Mermaid does NOT paint `.loopText`/`.sectionTitle` in source order — nested/inner blocks paint their labels FIRST (verified: source openers OUTER,INNER,P-A,LAST paint as INNER,P-A,OUTER,LAST). But a block's label always sits at the TOP of its box and section dividers never vertically cross, so **sorting the label elements by their on-screen `getBoundingClientRect().top` reproduces SOURCE order exactly**. `resolveSequenceBlockLabelTarget(clickedEl)` (in `useCanvasInteraction.ts`) Y-sorts all `.loopText` (resp. `.sectionTitle`) and indexes into the parser's source-ordered openers (resp. else/and/option dividers from `getSequenceBlockEntries().sections`), returning the absolute source `lineIndex`. The node id is `SEQ_BLK_<lineIndex>`.
  - **`rect` excluded**: `rect` highlights render NO `.loopText` (only a colored box, no text label), so the opener list filters `type !== 'rect'` to keep the Y-sorted ↔ source-order map 1:1. A label-less `loop`/`opt`/etc. STILL renders a `.loopText` (a zero-width space), so it stays in the map and is renamable (adds a label).
  - **Editor wiring**: handled at the TOP of `handleEditClick` (before the generic `getClickedNode` path so it never falls through to flowchart-node rename). It positions the shared `InlineTextEditor` over the clicked label's rect, sets `selectedNodeId = SEQ_BLK_<line>`, seeds `editingText` from the source label, and supports the same cross-element/commit transition as message/note editing. **`recalculateSelection` early-returns for `SEQ_BLK_`** — block labels have no persistent SVG element to re-resolve, so without that guard the "couldn't find element" branch would clear the selection box ~50ms after open and the editor would vanish mid-edit. Selection is cleared on submit.
- **Highlight (`rect`) Recolor (double-click)**: Double-clicking a `rect` highlight's colored background opens a small color popover (8 preset swatches + a native "Custom…" color input). Choosing a color rewrites ONLY the color argument after the `rect` keyword (`handleRecolorSequenceHighlight` in LiveMaidEditor: `/^(\s*)rect\b.*$/` → `${indent}rect ${color}`, custom hex converted to `rgb(r, g, b)`), preserving indentation; routes through `handleCodeChange` (single undo). Highlights carry no text label, so this is their analogue of the block-label rename. Implementation details:
  - **DETECTION MUST run through `handleEditClick`, NOT EditorCanvas's React `onDoubleClick` (CRITICAL)**: `react-zoom-pan-pinch` swallows the synthetic `dblclick` on SVG `<rect>` elements (the `TransformWrapper` intercepts it at its own capture listener even with `doubleClick.disabled`), so the canvas `<div onDoubleClick>` NEVER fires for a rect background — an early implementation there silently did nothing on every clean load (it only appeared to work under stale Fast-Refresh closures). The reliable path is the hook's document-level **capture-phase `dblclick`** listener (`handleNativeDblClick`) which drives `handleEditClick`; the rect recolor is therefore detected at the TOP of `handleEditClick` (same place as block-label rename and before `getClickedNode`). Do NOT move highlight detection back onto the React `onDoubleClick`.
  - **DOM→source mapping by Y-sort**: highlights render as `<rect class="rect" fill="rgb(...)">`; like block labels they are NOT painted in source order, but their TOP edges are strictly ordered, so `resolveSequenceHighlightTarget(clientX, clientY)` Y-sorts the `rect.rect` (rgb-fill) elements and indexes into the source-ordered `rect` blocks from `getSequenceBlockEntries`, returning `{ lineIndex, color }`. The hit rect itself is found via `document.elementsFromPoint` (class `rect` + rgb-ish `fill`).
  - **Popover ownership**: EditorCanvas owns the popover (needs `canvasShellRef`-relative coords + its own state), but the hook detects the dblclick — so the hook exposes `openHighlightRecolorRef` (a mutable ref) that EditorCanvas assigns an opener to via `useEffect`; `handleEditClick` calls `openHighlightRecolorRef.current(lineIndex, color, clientX, clientY)` when a highlight is hit. Closes on outside `mousedown`.
- **Lifeline Handle Overlay**: In sequence diagrams, hovering near a vertical lifeline surfaces a vertical stack of draggable `+` handles along valid anchor slots. Handles use a high-contrast indigo style with stronger icon weight and are scale-locked so they keep constant physical size while zooming. Handles are hover-driven (not persisted from selection) so they do not block participant click/double-click targets.
- **Sequence Note Actions in Floating Toolbar**: Selecting a sequence actor or message shows a Note button in `SequenceManipulationToolbar`. Clicking the Note button opens the Add Note dialog modal. Selecting a sequence note shows note-specific controls: `Change position` (left/right/over), `Link/Connect` placeholder action, and `Delete`. All add/move actions mutate Mermaid DSL immediately and re-render via the normal `handleCodeChange` path.
- **Message Type Mutation (inline operator swap)**: Selecting a sequence message (`SEQ_MSG_*`) shows a `Message Type` dropdown button in `SequenceManipulationToolbar` (between Rename and Delete; hidden for actors/notes). The dropdown lists four standard UML styles mapped to Mermaid operators: Solid+filled `->>`, Dashed+filled `-->>`, Solid+cross `-x`, Dashed+cross `--x`. The currently-active operator is highlighted (computed by `currentSequenceMessageOperator` in LiveMaidEditor via a `useMemo` that parses the selected message's pre-colon operator). Selecting an option calls `handleChangeSequenceMessageType(operator)` which: locates the selected message's source line (via `getSequenceMessageEntries`), splits at the first `:`, and swaps ONLY the operator in the pre-colon segment using `swapRe = /(<<-->>|<<->>|-->>|--x|--\)|-->|->>|-x|-\)|->)(\s*)(\S+)(\s*)$/` (anchored on the trailing receiver actor so a stray `-` in the sender id is never matched; `$2$3$4` preserve receiver + spacing). Sender, receiver, label text, and surrounding spacing are untouched. Routes through `handleCodeChange` so undo + re-render are normal; cycling all four types is idempotent (no code degradation). The dropdown and toolbar dismiss on outside click. **CRITICAL**: introducing the cross operators (`-x`, `--x`) required extending the actor-parsing regexes in `parseSequenceMessageActors` (useCanvasInteraction) and `getSelectedSequenceParticipantForNote` (LiveMaidEditor) to the full operator alternation, or participant/actor resolution breaks for cross-typed messages.
- **Note Hover Ring**: Hovering over a sequence note (`rect.note` or `.noteText`) shows the same indigo border + glow ring as sequence messages and flowchart nodes. Detection prefers `rect.note` directly; when hovering `.noteText`, the code walks up to the parent group to find the sibling `rect.note` for full-box bounds. The ring is suppressed while a note is already selected (`SEQ_NOTE_*`), during inline editing, or during an active connection drag.
- **Note Selection Box (Full Box)**: Clicking a sequence note selects it and outlines the full yellow note rectangle (`rect.note`), not just the text portion. The `textBox` (used for inline editing) still points to `.noteText` for accurate textarea placement. Both bounds are measured at click time via `getClickedNode` with a SEQ_NOTE_ special case, mirroring the SEQ_MSG_ paired-element pattern.
- **Note Position Syntax (CRITICAL)**: Mermaid uses two distinct syntaxes for note positions: `Note left of Actor: text` / `Note right of Actor: text` (with "of") and `Note over Actor: text` (without "of"). All note parsing (`getSequenceNoteEntries`) and mutation (`updateNotePosition`, `insertSequenceNoteAtIndex`) must respect this distinction. The parser regex uses `(?:of\s+)?` to accept both forms; the writer emits the correct form per position. `handleMoveSequenceNote` delegates to `updateNotePosition` to ensure a single source of truth.
- **Shared Insertion Lanes**: Sequence `+` handles are generated from a flattened global set of message rows and reused across all participant lifelines. Lanes are placed one above the first row, one between each adjacent row pair, and one below the last row, then clamped/deduplicated. The first lane is intentionally biased upward so the top insertion `+` is significantly higher and clearly visible across participant types. This guarantees one `+` per vertical gap, gives loop rows top+bottom insertion handles, and keeps participant columns visually aligned.
- **Contextual `+` Action Menu**: Clicking a lifeline `+` handle opens a compact two-action chip (Note + Self Loop) pinned to that lane and scale-locked with zoom. Choosing `Self Loop Message` immediately inserts `{actor}->>{actor}: new msg` at the clicked lane. Choosing `Note` keeps the chip (the "tool box") visible and expands a note-position chooser DIRECTLY BELOW it with direct actions (`Add note to the left/right/over`) — the chip is NOT replaced/hidden while the position selection is open (the active `Note` button is highlighted), and selection inserts `Note {position} of {participant}: new note` at the same lane. Both actions resolve insertion index from the clicked anchor Y, ensuring placement under the correct participant and in the correct chronological position.
- **Note Move Sub-Menu Keeps Toolbar (tool box stays)**: Selecting a sequence note shows the `SequenceManipulationToolbar` (Move + Delete). Clicking `Move` opens a note-position chooser (`Move note to the left/right/over`) rendered as a child popup directly below the toolbar, so the Move/Delete tool box REMAINS visible while the position selection is open. This mirrors the `+ > Note` flow. The popup is a self-managed `useState` panel (closes on outside `pointerdown` or selection change), replacing the previous `DropdownMenu` (which portaled away and visually detached from the toolbar).
- **Detailed Design Record**: The current model and regression guards for plus-button count/placement are documented in `reference/SEQUENCE_PLUS_PLACEMENT_SOLUTION.md`.
- **Horizontal Snap Drag Preview**: Dragging from a lifeline handle starts `connectionState` with a fixed anchor slot and renders a blue dashed horizontal preview with arrowhead (`y1 = y2`). During drag, the preview snaps to nearby lifelines and shows a green target indicator on the snapped slot.
- **Drag Indicator Coordinate Architecture (CRITICAL)**: The drag indicator SVG for sequence connections is rendered **outside** the `TransformWrapper` at the `canvasShellRef` level — the same pattern used by `sequencePlusMenu`. This is mandatory because any SVG inside `TransformWrapper` inherits the canvas coordinate system, which shifts if the canvas pans (even briefly). Placing the SVG outside means all coordinates are viewport-relative to `canvasShellRef`, completely immune to canvas pan/zoom transforms. Coordinates are computed directly in the native `window.mousemove` handler (`onMove`): `cursorX = ev.clientX - shellRect.left` (where `shellRect = canvasShellRef.current.getBoundingClientRect()`). Snap detection uses fixed 28 viewport-pixel proximity against `actor-line` element `getBoundingClientRect()` — no `scale` division is needed because everything is already in viewport space. The `x1`/`y1` origin is the `+` button's viewport position at mousedown (already stored as `anchorX`/`anchorMenuY` in viewport-relative coordinates).
- **Sequence Message Selection Visuals**: Clicking a sequence message selects the combined message region (line + label) using the outer selection frame. Hover still shows the outer purple ring for discoverability, and double-clicking the message starts inline label editing immediately.
- **Non-Blocking Message Trigger Layer**: Sequence message trigger rectangles are rendered as non-interactive (`pointer-events: none`) so they never swallow clicks intended for participant headers/footers. Message selection and rename continue through direct SVG targets (`.messageText` / `.messageLine0`) and the existing canvas hit-test path.
- **Full-Area Connection Band (Hover + Click)**: The entire message connection band — the rectangle spanning the message line and its label plus padding, not just the thin line/`.messageText` — is interactive for BOTH hover and click, mirroring the full-area note behavior. This is implemented via a live DOM hit-test `findSequenceMessageBandAtPoint(canvasX, canvasY)` in `useCanvasInteraction.ts` that computes each message's band bounds at interaction time (pairing each `.messageText` with its nearest `messageLine*` via `findNearestLineForText`) and returns the band whose vertical center is closest to the cursor (handles overlapping bands). Live DOM querying is used instead of precomputed React state because the trigger-area state is empty on cold load. **Hit-test padding is intentionally SMALLER than the visible box padding** — `SEQ_MSG_HITTEST_PADDING = {x:2, y:1}` (especially tight vertically) so the clickable/hoverable area is a touch smaller than the drawn box, preventing accidental clicks on adjacent message rows. Hover: the RAF-throttled React `onMouseMove` (`_handleMouseMoveInner`) AND the React `onMouseOver`/`onMouseOut` handlers (`handleSequenceHoverOver`/`handleSequenceHoverOut`, wired as props on the canvas container in `EditorCanvas.tsx`) resolve the bare SVG target to the band's `.messageText` so the indigo ring persists over empty band space (both paths must be band-aware or they clobber each other). Click: `handleSvgClick`'s empty-target fallback runs the same hit-test before clearing selection and selects the band's `SEQ_MSG_*` via `getClickedNode`. The trigger overlay divs remain `pointer-events: none` (Non-Blocking Message Trigger Layer guard intact).
- **Hover Wiring MUST Be React Props, NOT addEventListener (CRITICAL)**: Sequence message hover (`mouseover`/`mouseout`) is wired through React `onMouseOver`/`onMouseOut` props on the canvas container `<div>` (exposed from the hook as `handleSequenceHoverOver`/`handleSequenceHoverOut`), exactly like `onMouseMove`. A previous implementation used a manual `container.addEventListener('mouseover', ...)` inside a `useEffect`. Because the effect's dependency array did not (and cannot) react to `containerRef.current` mutations, whenever react-zoom-pan-pinch / conditional rendering replaced the container DOM node without changing the effect deps, the listener stayed bound to the now-detached old node and the LIVE container had ZERO hover listeners — the ring silently never appeared (confirmed via CDP `DOMDebugger.getEventListeners` returning `[]`). React-managed props automatically re-bind on every remount, so the hover ring stays reliable. Do NOT revert to addEventListener-in-useEffect for canvas hover.
- **Single Border Box (Hover == Click Geometry, CRITICAL)**: The hover ring and the click/selection box for a sequence message MUST be pixel-identical — one single border box across Default/Hover/Active states, differing only in color/emphasis, never in size/position. To guarantee this, BOTH boxes share the SAME module-level padding constant `SEQ_MSG_SELECTION_PADDING = {x:2, y:1}`: `hoveredSequenceMessageBox` (in `updateSequenceMessageHoverHighlight` and `triggerSequenceMessageHoverByIndex`) and the `selectionBox` geometry (`getClickedNode` + `recalculateSelection` `SEQ_MSG_` branches) all expand the raw line+label bounds by exactly `-SEQ_MSG_SELECTION_PADDING.x/.y` on origin and `+ *2` on size. Both boxes then receive the identical render offset/radius/border-width in `EditorCanvas.tsx` (`-4/scale` offset, `+8/scale` size, `6/scale` radius). Verified: hover box and click box reported identical bounds. Do NOT apply padding to only one of the two paths or use different padding values — the boxes will desync and the UI will appear to "jump" on click. (Note: the clickable hit-test uses a separate, smaller `SEQ_MSG_HITTEST_PADDING` — that is intentional and only affects the interactive area, never the drawn box.)
- **Connection Commit Rules**: Releasing on a snapped lifeline inserts a message line with default label `new msg`; releasing on empty canvas aborts cleanly with no code mutation. If source and target are the same actor, a self message (`{actor}->>{actor}: new msg`) is generated.
- **Anchor-Based Insertion Order**: New sequence messages are inserted by comparing the drag anchor Y against existing rendered `.messageText` positions. This updates message order by index instead of appending blindly.
- **Sequence Message Deletion Index Integrity**: Deleting a selected sequence message (`SEQ_MSG_*`) resolves the target by parsed message entries that skip frontmatter, then removes by absolute source line index. This prevents index drift where deleting a self-loop could accidentally remove another message (for example, the first `Alice->>Bob`).
- **Canonical Participant ID Mapping (Duplicate Alias Safe)**: Sequence interaction logic resolves participants by declaration order + lifeline X geometry, not alias text. This prevents collisions when multiple participants share the same alias (for example, multiple boundaries named `New Boundary`) and ensures top/bottom actor clicks map to the same underlying participant ID.
- **Actor Selection — Exact Box Preservation**: Sequence diagrams render each participant twice (header and footer). Selection now preserves the exact actor box the user clicks (top or bottom) instead of forcing selection to the header box.
- **Complex-Shape Twin Targeting (top vs bottom, CRITICAL)**: The specialised archetypes render as per-twin SVG GROUPS — Actor/Boundary/Control as `<g class="actor-man">`, Entity/Database/Queue as `<g class="actor">` — each with its OWN bounding box, and (for the complex types) the text label is a SEPARATE sibling, not a child of the group. Two resolution paths previously snapped a TOP-header click to the BOTTOM footer twin; both are fixed in `useCanvasInteraction.ts`:
  - **Initial bounds (`getClickedNode`)**: the actor branch no longer does a broad `currentNode.parentElement.querySelector('rect.actor')` (which, for a complex group whose parent contains other actors/twins, grabbed the FIRST `rect.actor` in document order — the wrong actor or twin). It now measures the EXACT clicked element: if `currentNode` is the complex group itself (`g.actor`/`g.actor-man`) or a `rect.actor`, measure its own `getBoundingClientRect()`; only when a narrow `<text>` label is clicked does it use a `:scope > rect.actor` SIBLING (scoped to the tight `<g>` so it can't reach into another actor/twin), falling back to the text's own box.
  - **Recalc/preserve effect (`resolveSelectionForSelectedNode`)**: the clicked element gets a twin-unique id (`seq-actor-<id>-<left>-<top>`, embeds its position). The exact-id round-trip preservation guard previously required `exactEl.classList.contains('actor')`, which is FALSE for `actor-man` stick-figure groups, so Actor/Boundary/Control fell through to geometry that picked the wrong twin. The guard now accepts `actor` OR `actor-man`, and the geometry candidate query is `.actor, .actor-man` so a complex shape resolves to itself.
  - Verified on tall-lifeline twins: clicking each twin (top AND bottom) of Actor/Database lands the selection box on the clicked instance (selection cy == clicked cy, never the opposite twin); the inline rename editor anchors on the clicked top instance and committing updates BOTH rendered twins + the single code declaration (twin metadata stays synced while focus stays isolated).
- **Actor Selection — Label-First Resolution (with Ambiguous-Label Geometry Override)**: Click-hit selection for sequence actors resolves participant IDs by normalized label text first (case-insensitive, whitespace-collapsed, and multi-`tspan` safe), with geometric nearest-lifeline mapping only as fallback. This prevents clicks on labels like `New Interface`/`New Database` from accidentally selecting another actor (for example, `Alice`). **CRITICAL exception — duplicate labels**: when MULTIPLE participants share the same display label (e.g. two participants both aliased `New Boundary`, with distinct ids `P765` and `D`), label-first resolution always returns the FIRST declared match, so clicking the RIGHT `New Boundary` wrongly selected the LEFT one. `getClickedNode`'s actor branch now counts how many participant declarations match the clicked label (`getSequenceParticipantEntries` filtered by normalized alias OR id); if the count is >1 the label is ambiguous and the clicked X's **nearest-lifeline geometry wins** instead of label-first. Unique labels keep the robust label-first path. Verified: left `New Boundary` → `SEQ_ACTOR_P765`, right `New Boundary` → `SEQ_ACTOR_D` (previously both → `P765`).
- **Full-Shape Participant Hitbox (complex archetypes)**: Standard `participant`/`collections`/`queue` render as solid `<rect class="actor">` whose fill captures clicks, but the specialised archetypes render as `fill:none` SVG groups — Actor/Boundary/Control as `<g class="actor-man">` and Entity/Database as `<g class="actor">` — so clicks in their interior whitespace previously passed through to the canvas (only the literal strokes/text were clickable). Fix has three parts: (1) **CSS** (`globals.css`): `.mermaid-container g.actor, .mermaid-container g.actor-man { pointer-events: bounding-box; cursor: pointer; }` makes the browser hit-test the ENTIRE geometric bounding box of each shape group (interior included). (2) **Click resolver** (`getClickedNode`): the actor branch matches BOTH `actor` AND `actor-man` (the stick-figure groups carry only `actor-man`, not the bare `actor` class). `actor-line` lifelines are NOT matched (token match, never equals `actor-line`); actor id resolves via `getSvgTextDisplayName` then geometric nearest-lifeline fallback (the group's bbox center x == lifeline x). The hover path (`_handleMouseMoveInner`) likewise uses `closest('.actor, .actor-man')`. (3) **Lifeline `+` overlay suppression**: the narrow complex-shape icons (≈10–24px wide) sit under the topmost lifeline `+` button (`seq-connect-btn`, 24px, centered on the lifeline x), which would intercept the click. So when the cursor is directly over any participant shape (`rect.actor`, `g.actor`, `g.actor-man`, `text.actor` — viewport-rect hit-test), the `sequenceLifelineOverlay` is cleared, keeping the header freely clickable for SELECTION; the `+` affordance still appears along the lifeline body away from the header. Verified: all 5 complex types select on an interior click with code intact across fresh loads.
- **Participant Visual Affordance (Hover + Selected)**: Sequence participants render circular visual indicators on hover and when selected, improving discoverability and making participant-selection state obvious during editing.
- **Participant Type Picker**: A compact grid dropdown (w-56, 3 columns, 8 types) appears directly below the Participants button in the toolbar. Available types: `participant`, `actor`, `boundary`, `control`, `entity`, `database`, `collections`, `queue`. Non-standard types use Mermaid's `@{ "type": "..." }` syntax.
- **Participant Add — Right-Side Insertion + Auto-ID (`handleAddParticipant` in `sequence.tsx`)**: Adding a participant from the top "Participants" dropdown places the new column on the FAR RIGHT (rightmost), never the far left. Mermaid orders participant columns by FIRST APPEARANCE, so the new declaration must come AFTER every existing participant: (1) if explicit `participant`/`actor`/typed declarations exist, the new line is spliced directly beneath the LAST declaration line (`lastDeclIdx + 1`) — clean code, renders rightmost; (2) if participants are only IMPLICIT (introduced via messages like `A->>B`, no declaration lines), the new line is appended at the END of the code so it appears after all message-referenced columns (declaring it at the top would wrongly make it the far-LEFT column — the original bug). Auto-ID (`AC 1.3`): scans every existing id (declaration ids + message sender/receiver ids) and picks the first unused single uppercase letter `A`→`Z` (e.g. `D` when `A,B,C` exist), falling back to `P{last-3-digits-of-Date.now()}` only if all 26 are taken. Write format matches the canvas type mutation: `participant`/`actor` keyword form, others `participant ID@{ "type": "X" } as New {Label}`. Routes through `setCode` (single undo). Verified: implicit diagram → appended at end, order `A,B,C,New Database`; explicit diagram → injected at line beneath last `participant`, order `Alice,Bob,Charlie,New Queue`; ids increment `D`,`E` across successive adds.
- **Participant Type Mutation on Canvas (inline shape picker, Bi-directional Sync)**: Selecting a participant header (`SEQ_ACTOR_*`) shows a `Type` button (`Shapes` icon) in `SequenceManipulationToolbar`, between Rename and Delete (hidden for messages/notes). Clicking it opens a 4-column icon grid of the eight archetypes (`participant`, `actor`, `boundary`, `control`, `entity`, `database`, `collections`, `queue`) with hover + focus-visible states; the current type is highlighted (indigo border/ring). Choosing one calls `onChangeSequenceParticipantType(typeKey)` → `handleChangeSequenceParticipantType` in LiveMaidEditor, which finds the participant's declaration line by id and rewrites ONLY the type flag, preserving id + alias + indentation. Write format mirrors the top "Participants" picker (`handleAddParticipant`): `participant`/`actor` use Mermaid keyword syntax (`participant ID as Alias` / `actor ID as Alias`, dropping any prior `@{}`); all other types use the metadata form `participant ID@{ "type": "X" } as Alias` (no space before `@`). Routes through `handleCodeChange`, so the change is a single Monaco undo transaction (Ctrl/Cmd+Z reverts both code and the rendered shape) and Mermaid re-renders the new shape in place — the lifeline x and message vectors are unchanged (Mermaid owns that geometry; we only mutate the type token), and the label re-centers inside the new box automatically. Implementation details:
  - **Shared assets**: `ParticipantIcon` (parametrized with an optional `className`) and `PARTICIPANT_TYPES` are exported from `src/lib/diagrams/sequence.tsx` and reused by BOTH the top picker and the inline toolbar grid. `SequenceManipulationToolbar` importing from `sequence.tsx` is a one-way dependency (sequence.tsx imports nothing from the editor components), so there is no import cycle; a transient `PARTICIPANT_TYPES is not defined` error seen mid-edit was just HMR evaluating a partial module and cleared on full reload.
  - **Dropdown reliability**: toggles on `onMouseDownCapture` (capture-phase) like the Message Type / Move dropdowns (a transient toolbar reflow can swallow the native click); closes on outside `pointerdown` and on `selectedNodeId` change.
  - **Parse regex** (`SEQ_PARTICIPANT_DECL_RE` in LiveMaidEditor): `^(\s*)(?:participant|actor|boundary|control|entity|database|collections|queue)\s+(\S+?)(?:\s*@\{[^}]*\})?(\s+as\s+.+?)?\s*$` — g1 = indent, g2 = id (matched against the selected actorId), g3 = ` as Alias`. The keyword and old `@{}` token are intentionally discarded and rewritten. The active-type read-back (`currentSequenceParticipantType` memo) extracts `"type":"X"` from the `@{}` token if present, else falls back to the keyword.
  - Verified end-to-end on a temp diagram: `participant C as Charlie` → Database renders a cylinder header (top+bottom) with the lifeline unmoved and code `participant C@{ "type": "database" } as Charlie`; undo restores `participant C as Charlie` in one step; Actor → `actor C as Charlie`; the active archetype is highlighted in the grid.
- **Sequence Note Dialog Modal**: Clicking the Note button in the toolbar opens a modal dialog titled "Add Note" with three button options for position selection: "Note to the left", "Note to the right", and "Note over". The selected button is highlighted with default styling. Clicking "Add Note" confirms the selection and closes the dialog, or "Cancel" dismisses without adding. The dialog defaults to "Note to the right" position on each open.
- **Adaptive Lifeline Hover Threshold (Dense Diagram Support)**: The lifeline hover detection uses an adaptive threshold rather than a fixed 44px tolerance. The minimum inter-lifeline spacing is computed from sorted lifeline X coordinates, and the effective `hoverThreshold` is clamped to `min(44, minSpacing * 0.45)`. Hit-testing uses a nearest-match `reduce` (not `find`) so the geometrically closest lifeline always wins — ensuring correct hover even when participants are packed as close as 18px apart (e.g. 16-participant diagrams).
- **Flat-Surface (Note-Independent) Grid for Lifeline `+` Buttons** (`getSequenceAnchorSlots` in `useCanvasInteraction.ts`): All lifelines are treated as a single flat plane. The slot grid (one slot above the first row, one midpoint between each adjacent pair of message rows, one slot below the last row) is derived **purely from the shared global message rows**, so it is **identical for every lifeline** — the "Order 1 rule" applied everywhere. **Notes have ZERO effect on placement**: their presence or absence never inserts, removes, shifts, or resizes a slot, so the purple `+` buttons keep the exact same vertical alignment on every lifeline whether or not notes are loaded. The first slot is clamped only to `[globalTop, end]` (so a `+` always appears above the first message); all other slots clamp to `[start, end]`; the list is sorted and deduplicated. **Do NOT reintroduce any note-avoidance / `pushBelowNotes` / `noteRanges` logic** — it breaks the flat-surface guarantee by making columns drift relative to one another (verified: Tax, Order 1, and Fraud all return identical slots `[7, 20, 32, 36, 41, 47, …]`).
- **First `+` Slot Cross-Lifeline Sync**: Message row collection uses `centerY >= globalTop` (the actor box top boundary) as the lower bound instead of `centerY >= start` (the lifeline start below the actor box). This ensures the first message row is never missed when it falls inside the actor box's vertical extent (possible on dense/zoomed-in diagrams). The first slot above that row is then clamped to `max(globalTop, ...)` — the same `globalTop` for every participant — so all lifeline columns display their topmost `+` button at an identical Y position, keeping the row grid visually aligned.
- **Inline Editor Coordinate Offset (Scale-Invariant)**: `InlineTextEditor` positions its `<textarea>` relative to the selection box div. The selection box div is rendered at canvas position `(selectionBox.x − 4/scale, selectionBox.y − 4/scale)`. The editor must use the same scale-divided offset: `offsetCanvas = 4 / scale`, then `relativeNodeX = textBox.x − (selectionBox.x − offsetCanvas)`. Using a hardcoded `4` instead of `4/scale` causes a growing vertical error at high zoom (e.g. actor boxes are ~5 canvas units tall; a 4-canvas-unit fixed offset at zoom 15× pushes the editor well outside and below the selection box). The scale-invariant form keeps the textarea centered inside the actor label at any zoom level.
- **Message Reorder Drag-and-Drop (Bi-directional Sync)**: A selected sequence message (`SEQ_MSG_*`) can be dragged vertically to reorder it; the Mermaid code is spliced in sync and Mermaid re-renders (autonumber recalculates automatically). Implementation:
  - **Drag handle overlay**: When a message is selected, `EditorCanvas` renders a `pointer-events-auto` div with class `seq-msg-reorder-handle` over the message's `selectionBox` (canvas space). The class is added to react-zoom-pan-pinch `panning.excluded` (alongside `seq-connect-btn`) so the initial mousedown never starts a canvas pan; `panning.disabled` also includes `!!seqReorder` once the drag is active. The same handle forwards `onDoubleClick` to `onHoveredSequenceMessageDoubleClick(index)` to preserve double-click-to-edit.
  - **Easy-grab UX (hover grab, no select-first)**: A SECOND `seq-msg-reorder-handle` overlay is rendered over the HOVERED message band (`hoveredSequenceMessageBox`) for the hovered message REGARDLESS of selection — so the user can grab-and-drag ANY message directly on hover without a prior click, even when a different message is selected. Both overlays use a generously ENLARGED grab area (`−8/−5` px origin, `+16/+10` px size at scale) so the thin ~9–21px message band is easy to hit. The drag threshold is 3px. On the hover overlay, `onMouseDown` calls `startSeqReorderDrag(e)`.
  - **Dragged message = the one under the cursor, NOT the selection (CRITICAL)**: `startSeqReorderDrag(e, fromIndexArg?)` resolves the dragged `fromIndex` as explicit arg → nearest band to the cursor at mousedown. It deliberately does NOT prefer `selectedNodeId`: if message X is selected and the user grabs a DIFFERENT message Y via the hover overlay, Y must be dragged, not X (an earlier version preferred the selection and dragged the wrong message). On drag start, if a DIFFERENT message is selected, `onDeselect()` is called so the stale selection box/toolbar does not linger during the drag.
  - **Unified message + note reordering**: Messages AND notes are draggable and share ONE unified ordered row list. `startSeqReorderDrag` builds `rows` from both `.messageText` (band = text ∪ arrow line) and `.noteText`/`rect.note` (band = the note box), sorted top-to-bottom (== source order). A row can be dropped into ANY gap (message- or note-adjacent). The dragged row is the one under the cursor; on drop it calls `onReorderSequenceItem({ kind: 'msg' | 'note', index: domIndex }, toSlot)`. The commit (`handleReorderSequenceItem` in LiveMaidEditor) builds a unified entry list (message entries + note entries sorted by source line), maps the dragged item + `toSlot` to source lines, and splices the single line — so unrelated lines (blocks, participants) stay put. Routes through `handleCodeChange` (undo + autonumber). The no-drag mouseup select/edit path is per-kind: messages → `onHoveredSequenceMessage{Click,DoubleClick}`, notes → `onHoveredSequenceNote{Click,DoubleClick}` (the latter call `triggerHoveredSequenceNoteSelection(startEdit, index)` in the hook, which selects/edits a note by `.noteText` DOM index). The double-click timing ref (`seqLastClickRef`) is keyed by `${kind}:${domIndex}`.
  - **Empty-block prune after reorder (CRITICAL — prevents diagram break)**: Dragging a message/note OUT of a block can leave that block with no content — e.g. dragging the SOLE message out of a `rect … end` highlight (or `loop`/`opt`/`alt`/`par`/`critical`/`break`) leaves an opener immediately followed by its `end`. An empty block PARSES (`mermaid.parse` is lenient) but CRASHES `mermaid.render` during sequence bounds calculation, breaking the ENTIRE diagram (blank canvas / error). So after the splice, `handleReorderSequenceItem` runs `pruneEmptySequenceBlocks(lines)` (module-level in LiveMaidEditor): a stack-based pass that drops any block whose body has NO real content (only section dividers `else`/`and`/`option`), removing the opener line, its section-divider lines, and the matching `end`. Content propagates to parents (a parent containing only empty children is itself pruned, so one pass handles arbitrary nesting). ANY non-empty, non-structural line counts as content, so blocks that still hold something are never touched. Verified: dragging the lone message out of a `rect` (its only child) auto-removes the now-empty `rect … end` and the diagram keeps rendering instead of crashing. Do NOT remove this prune — naive line-splice reordering across block boundaries is otherwise unrenderable.
  - **Note hover MUST use the onMouseOver path, not just mousemove (CRITICAL)**: Message hover works via BOTH `handleSequenceHoverOver` (React `onMouseOver`) and the RAF-throttled mousemove inner handler. Note hover originally only lived in the mousemove inner handler — which in practice does not reliably deliver the sequence branch (message hover visibly works purely through `onMouseOver`), so the note grab overlay never appeared. Fix: `updateSequenceNoteHover(clientX, clientY)` is called from `handleSequenceHoverOver`/`handleSequenceHoverOut`, doing a viewport-coordinate hit-test against `rect.note` boxes (coordinate-based, NOT `e.target`-based) so it stays stable even when the note's pointer-events:auto grab overlay covers the note and changes `e.target`. Do NOT rely solely on the mousemove path for note hover.
  - **Click/double-click handled on MOUSEUP, not the `click` event (CRITICAL)**: When the overlay receives a real `mousedown`, the resulting state churn (hover update + selection mounting the second handle) re-renders and REPLACES the overlay DOM node between `mousedown` and `mouseup`, so the browser never fires a native `click` on a single stable node (confirmed: `mousedown`+`mouseup` fire but no `click`). Therefore select-vs-edit is resolved inside `startSeqReorderDrag`'s window `mouseup` (`onUp`) handler when no drag occurred: it calls `onHoveredSequenceMessageClick(fromIndex)` to select, or `onHoveredSequenceMessageDoubleClick(fromIndex)` to edit. Double-click is detected by TIMING (`seqLastClickRef`): two no-drag mouseups on the same message index within 350ms ⇒ edit. This is robust against the DOM node replacement that breaks both native `click` and `e.detail`. The hover grab overlay renders for the hovered message REGARDLESS of selection so EVERY message's mousedown registers this `onUp` path (only the VISIBLE hover ring keeps the `!selectedNodeId?.startsWith('SEQ_MSG_')` condition to avoid a double ring). Do NOT move this logic back onto `onClick`/`onDoubleClick`.
  - **CRITICAL — capture-listener bail**: `EditorCanvas`'s document-level capture `mousedown` listener (`onDocumentMouseDownCapture`) MUST treat `.seq-msg-reorder-handle` as floating UI (`hitFloatingUi`), otherwise it routes the mousedown into `handleSvgClick` and deselects the message mid-drag (handle vanishes). Do NOT remove this guard.
  - **Viewport-space visuals**: All drag geometry (drop-zone bands + cursor ghost) is computed in viewport space relative to `canvasShellRef` from live DOM rects (`.messageText` centers for slot Ys, `line.actor-line` extent for horizontal span) and rendered OUTSIDE `TransformWrapper` — the same robustness pattern as `seqDragIndicator`. Panning is disabled during the drag so coordinates never shift.
  - **Slot model**: N messages → slots `0..N` (0 above first, `k` between `k-1`/`k`, `N` below last). Drop zones render for every slot EXCEPT the dragged message's own gaps (`from` and `from+1`). The nearest slot to the cursor highlights (solid indigo); others are dashed hatched bands. Drop is valid only when the cursor is within `[slotY(0)−28, slotY(N)+28]` and near the diagram width; otherwise the drop cancels and the message snaps back (no code mutation).
  - **Strict hitbox drop detection (cancel on invalid drop, CRITICAL)**: A reorder commits ONLY when the cursor falls INSIDE a drop zone's bounding box — `findTarget` tests explicit intersection `cursorY ∈ [s.y − (s.h/2 + 6), s.y + (s.h/2 + 6)]` AND `cursorX ∈ [left − 24, left + width + 24]`, returning `null` (→ abort/snap-back) when the drop lands in the dead space between zones, in the dragged message's own excluded gaps, or off to the side. This replaced an earlier nearest-neighbor implementation that rounded any drop within a broad `[minY−28, maxY+28]` band to the closest slot, causing false-positive reorders (a drop near the original position would still snap to a zone). The SAME `findTarget` drives both the live preview highlight and the commit, so a highlighted zone honestly means "drop here will land"; no highlight means the drop will cancel.
  - **CRITICAL — interstitial Y from message BANDS, not text midpoints**: Each message occupies a vertical BAND = its `.messageText` label UNION its arrow line (`messageLine*`). In Mermaid the arrow line sits BELOW the text label, so the empty gap between two messages is `band[k−1].bottom … band[k].top` — NOT the midpoint between text centers (that midpoint lands directly on the upper message's connection line). The slot Y is the center of that empty gap, and each zone's HEIGHT is clamped to `emptyGap * 0.7` (5–14px) so it never touches a line/label. Text↔line pairing uses the SAME scoring heuristic as the hook's `findNearestLineForText` (`dy*3 + dx + (lineAboveText?60:0)`) — a naive nearest-by-center pairing mis-assigns around self-loops (tall arcs, height > 12px) and corrupts neighboring bands, causing zones to render on top of flat arrows. Verified: 0 zones overlap any text center or line center (flat arrows and self-loop arcs included).
  - **Commit (`handleReorderSequenceMessage` in LiveMaidEditor)**: Resolves `fromIndex` and `toSlot` against `getSequenceMessageEntries(code)` (raw source line indices, frontmatter-skipping). The single source line is removed and re-inserted relative to the OTHER message lines (interleaved notes/blocks stay in place). No-op when `toSlot === from || from+1`. Routes through `handleCodeChange`, so undo/redo (Monaco) and autonumber behave exactly like every other canvas mutation. Selection is cleared after a successful move. Works for all message types including self-loops (`A->>A`).
- **Participant Lifeline Reorder — Direct Horizontal Drag (Bi-directional Sync)**: Drag a participant HEADER block left/right to reorder the lifeline columns; the `participant` declaration order in the code rewrites to match (declaration order top-to-bottom == canvas order left-to-right). Direct-drag on the body (no handle bar), mirroring the message reorder but on the X axis. Implementation:
  - **Grab overlay**: a `seq-actor-reorder-handle` `pointer-events-auto` div is rendered over the hovered actor header (and the selected actor's `selectionBox`), `cursor-grab`. The class is in react-zoom-pan-pinch `panning.excluded`; `panning.disabled` also includes `!!seqLifelineReorder`. It does NOT block click-select / double-click-edit: those flow through the existing document-capture mousedown and native dblclick handlers, which resolve the actor SVG BEHIND the overlay via `elementsFromPoint`. CRITICAL: the document-capture target resolver was updated to skip `.seq-actor-reorder-handle` (`elements.find(el => container.contains(el) && !el.closest('.seq-actor-reorder-handle'))`) so the actor behind the overlay is found — otherwise the overlay div itself resolved to a non-actor target and the click deselected.
  - **Actor hover is COORDINATE-based** (not `e.target.closest('.actor')`): because the grab overlay covers the header, `e.target` becomes the overlay div and a `closest('.actor')` lookup would miss → the hover box flickers off → the overlay unmounts → flicker loop. `_handleMouseMoveInner` now hit-tests `rect.actor`/`g.actor`/`g.actor-man` bounds by viewport coordinates (smallest containing box wins), keeping the box stable under the overlay (same technique as note hover).
  - **Drag (`startSeqLifelineDrag` in `EditorCanvas`)**: runs in viewport/shell space (`canvasShellRef`-relative), pan/zoom-immune. Calls the new `getSequenceLifelines` prop (canvas coords, sorted L→R, includes implicit participants resolved by label/geometry) → converts x to shell via `toShellX`. The dragged actor = lifeline nearest the mousedown x. Slots `0..N` sit at inter-column gaps (before first / between adjacent / after last), excluding the dragged column's own two (`fromIndex`, `fromIndex+1`). 3px intent threshold so a sub-threshold press stays a plain click. Strict hitbox: a drop commits only when the cursor is within a slot's x tolerance AND inside the lifelines' vertical span; otherwise it aborts. Vertical dashed drop-zone BANDS + a vertical cursor guide render outside `TransformWrapper`.
  - **Commit (`handleReorderSequenceLifelines(newOrderIds)` in LiveMaidEditor)**: Mermaid lays columns out in FIRST-APPEARANCE order, so to force an arbitrary order EVERY lifeline is declared explicitly at the top in `newOrderIds` order: each participant's EXISTING declaration line is reused verbatim (keyword + `@{type}` + ` as Alias` preserved); a plain `    participant <id>` is synthesized only for implicit participants that had no declaration. Old declaration lines are removed and the new block is spliced in at the first-declaration position (or right after `sequenceDiagram` + optional `autonumber`/frontmatter when the diagram had only implicit participants). ONLY the declaration order changes — message lines, blocks, notes, and the logical flow are untouched (message arrows re-route automatically on Mermaid re-render). Routes through `handleCodeChange` (single undo). Verified: explicit reorder (`A,B,C`→`B,C,A`), middle insert (`A,B,C`→`A,C,B`), implicit diagram synthesizes `participant B/C/A`, undo restores in one step, and click-select + double-click-rename still work over the grab overlay.
- **Message Endpoint Drag-and-Drop (Reassign Sender/Receiver, Bi-directional Sync)**: When a sequence message (`SEQ_MSG_*`) is selected, two vertical rounded-capsule ("bar") drag handles appear at the message's source (sender) and target (receiver) endpoints. Dragging a handle onto a different participant lifeline rewrites that endpoint in the Mermaid code (e.g. `A->>B: msg` → `A->>C: msg`), preserving the operator, label, and the other endpoint. Cross↔self morphing falls out for free: dragging a receiver onto the sender's own lifeline yields `A->>A` (self-loop), and dragging a self-loop endpoint to another lifeline yields a cross message. Implementation:
  - **Endpoint geometry (`getSequenceMessageEndpointGeometry(idx)` in `useCanvasInteraction.ts`)**: Returns `{ from, to, isSelf, source:{x,y}, target:{x,y}, lifelines:[{actorId,x}] }` in **canvas coordinates** (pre-transform). Source/target x come from the actual lifeline positions (`getSequenceLifelines`, declaration-order ↔ sorted-x mapping), the y from the paired `messageLine*` rect (`findNearestLineForText`). For self-loops, source = loop top, target = loop bottom, both on the actor's own lifeline. Canvas coords are transform-invariant, so the handles are rendered INSIDE `TransformComponent` and track pan/zoom WITHOUT recompute; the memo (`selectedSeqMsgEndpoints` in `EditorCanvas`) only re-runs on `[selectedNodeId, svgContent, code]`.
  - **Handles render**: Two `pointer-events-auto` `seq-endpoint-handle` vertical capsules (white fill, 3px blue border, `17×54` px base size, z-[24] — above the reorder grab overlay z-21 and selection border z-22). Each is positioned at its endpoint x (`left: pt.x`) and CENTERED vertically ON the endpoint line (`top: pt.y`, no fixed upward nudge). A constant upward offset was removed because at zoom-OUT the message rows pack to a few screen-px apart while the bar keeps a minimum on-screen size, so any upward shift drifted the bar onto the PREVIOUS message — centering on its own endpoint keeps it unambiguous at every zoom. The endpoint coords drive `startSeqEndpointDrag`, so the rendered position never affects drag accuracy. `seq-endpoint-handle` is added to react-zoom-pan-pinch `panning.excluded`; `panning.disabled` also includes `seqEndpointDragging` while a drag is active.
  - **Zoom-aware sizing (CLAMPED scale-lock — the "size/zoom in/out" fix)**: The bars use the `data-scale-lock-max1` clamped variant (see §3), NOT the plain `data-scale-lock`. Counter-scale factor = `Math.min(1, 1/state.scale)`: when zoomed IN the bar stays a CONSTANT on-screen size (`17×54` px — won't balloon); when zoomed OUT it SHRINKS WITH the diagram (e.g. ~`9×16` px at 0.5×) so it never dominates a densely-packed min-zoom diagram (the original bug: a constant 44px bar spanned ~8 message rows at smallest zoom). Combined with the centered `top: pt.y` placement, the bar always straddles its own endpoint symmetrically and never drifts onto a neighboring row. Verified on `test-seq-diagram`: bars centered on the endpoint line (handle cy ≈ line y) at scale 0.5×, 1×, and 4.7×, staying lifeline-aligned throughout. Because zooming auto-deselects the message (§3 "Auto-Deselection on Zoom"), the handles unmount during a zoom gesture, so the React-rendered inline `scale(min(1,1/state.scale))` is the source of truth on re-select; `updateScaleLockedElements` keeps it consistent via the `[data-scale-lock-max1]` branch. Do NOT use plain `data-scale-lock` or an unclamped `1/scale` here, or the bars balloon at min zoom.
  - **Drag (`startSeqEndpointDrag` in `EditorCanvas`)**: Runs entirely in **viewport/shell space** (`canvasShellRef`-relative), mirroring the lifeline `+` connection drag, so pan/zoom never distorts coordinates. The dragged endpoint's **Y is LOCKED** to the message row (only X moves, preserving chronological order); X **snaps** to the nearest `line.actor-line` within 44 shell-px. Snap targets recover each `actorId` by zipping the live DOM actor-lines (sorted L→R) with `geo.lifelines` (also sorted L→R) — a positional zip, exact and name-ambiguity-free. The dashed ghost line + green snap dot reuse the existing `seqDragIndicator` overlay (rendered outside `TransformWrapper`). canvas→shell conversion: `shellX = canvasX*scale + containerRect.left − container.scrollLeft − shellRect.left`.
  - **Commit (`handleChangeSequenceMessageEndpoint(endpoint, newActorId)` in LiveMaidEditor)**: Splits the message line at the first `:`, matches the pre-colon segment with `/^(\s*)(\S+?)(\s*)(<<-->>|<<->>|-->>|--x|--\)|-->|->>|-x|-\)|->)(\s*)(\S+)(\s*)$/`, and swaps ONLY the sender or receiver slot — operator, the other actor, label (everything from `:` on), and all spacing are untouched. No-op if the actor is unchanged. Routes through `handleCodeChange` so undo/redo registers the drag as a single Monaco transaction (Ctrl/Cmd+Z reverts both code and canvas). Self vs cross is implicit: when sender === receiver the resulting `A->>A: msg` is a valid Mermaid self-message.
  - **CRITICAL — lazy sender group in actor-parsing regexes**: The sender capture MUST be lazy (`\S+?`), NOT greedy (`\S+`). A greedy sender swallows the leading dash of a double-dash operator — `B-->>A` parses as sender `B-` + op `-->>` (the first successful backtrack position), breaking lifeline resolution and the endpoint mutation for ALL dotted/cross messages (`-->>`, `-->`, `--x`, `--)`). Single-dash ops (`->>`, `->`, `-x`, `-)`) happen to be safe because the intermediate backtrack positions (`>>`, `>`) are not valid operators. This latent bug pre-existed in `parseSequenceMessageActors` but only surfaced once endpoints were drawn for reverse/dotted messages. Both `parseSequenceMessageActors` (hook) and the endpoint mutation regex (LiveMaidEditor) use the lazy form.
- **Interactive Logic Blocks & Highlights — Phase 1 (Contextual Add + Visual Overlays)**: Sequence diagrams render translucent labelled overlays for the structured-fragment blocks (`loop`, `alt`, `opt`, `par`, `critical`, `break`) and for `rect` highlight boxes, and a contextual `+ > Logic` menu inserts the block boilerplate. This is the foundation phase of the "Interactive Visual Logic Blocks & Highlights" epic (later phases: scope resizing, moving messages in/out, deep-nesting validation). Implementation:
  - **Block AST parser (`getSequenceBlockEntries(sourceCode)` in `useCanvasInteraction.ts`)**: A stack-based parser that returns `SequenceBlockEntry[]` — `{ id, type, isHighlight, label, startLine, endLine, depth, sections }`. `openerRe = /^(loop|alt|opt|par|critical|break|rect)\b\s*(.*)$/i`, `sectionRe = /^(else|and|option)\b/i` (mid-block dividers), `closerRe = /^end\b/i`. Nesting `depth` comes from the open-block stack; `id = SEQ_BLOCK_<openerLineIndex>`. `rect` is flagged `isHighlight: true` (amber styling). Frontmatter lines are skipped. Output is sorted by `depth` then `startLine` so parents paint before children (outer overlays sit behind inner ones).
  - **Geometry effect (canvas coords)**: A DOM-driven effect computes one `SequenceBlockArea` (`{...entry, x, y, width, height}`) per block in **canvas coordinates** (transform-invariant), so overlays are rendered INSIDE `TransformComponent` and track pan/zoom WITHOUT recompute. Vertical extent: the effect builds `rows` from rendered message rows (`.messageText` ∪ nearest `messageLine*` via `findNearestLineForText`, paired to source lines via `getSequenceMessageEntries`) plus note rows (`rect.note`/`.noteText` paired to `^note` source lines); a block's top/bottom span the rows whose SOURCE line falls in `(startLine, endLine)`. Empty blocks fall back to the gap between neighbouring rows. Horizontal: spans all lifelines (`getSequenceLifelines` min/max x) inset by depth — `padX = max(4, 18 − depth*10)` — so nested children sit visibly inside parents. `headroom = 18`, `footer = 10`.
  - **CRITICAL — cold-load rAF retry (overlays never appear without it)**: `react-zoom-pan-pinch`'s `<TransformComponent>` mounts its children (and thus attaches `containerRef`) a frame AFTER `svgContent` is first set on cold load, so the geometry effect fires with the SVG ready but `containerRef.current` still `null` (and `getSequenceLifelines()` empty). Ref attachment does NOT re-trigger effects and `svgContent` doesn't change again, so a one-shot effect would leave `sequenceBlockAreas` empty until an unrelated re-render (e.g. toggling autonumber). The effect therefore retries on `requestAnimationFrame` (bounded `MAX_ATTEMPTS = 150` ≈ 2.5s — the container was observed attaching at ~attempt 65 / ~1.1s) until the container + lifelines are measurable, then computes once and stops. On warm re-renders `compute()` succeeds on attempt 0 (no looping). The rAF id is cancelled in the effect cleanup. **This same latent cold-load race affects the pre-existing `sequenceMessageTriggerAreas` effect** (which is why the message hit-test queries the live DOM instead of that state); do NOT remove the rAF retry or the overlays regress to "only appear after you interact". The overlays' borderWidth/label use the standard scale-lock pattern so they keep constant on-screen weight across zoom.
  - **Contextual `+ > Logic` menu (US1)**: The lifeline `+` action chip gains a `Logic` button (`GitBranch` icon, gated by the `onSequencePlusBlock` prop) alongside Note/Self-Loop. It toggles `sequencePlusMenu.mode` to `'logic'`, expanding a submenu DIRECTLY BELOW the chip (tool box stays visible, same pattern as the Note sub-menu) listing the six logic blocks + a divider + `Highlight Box` (→ `rect`). Choosing one calls `onSequencePlusBlock(anchorY, type)` → `handleSequencePlusBlock` in LiveMaidEditor, which resolves two lifelines (`getSequenceLifelines()[0]/[1]`, fallback `A`/`B`), builds the per-type boilerplate body (each wraps a `${a}->>${b}: Message`), and inserts it at the anchor-resolved chronological index (`getSequenceInsertIndexForAnchor`). Routes through `handleCodeChange` (single undo). Overlays appear immediately because the geometry effect re-runs on the resulting `code`/`svgContent` change.
  - **Styling**: Logic blocks = violet (`rgba(124,58,237,0.5)` border, `rgba(139,92,246,0.07)` fill) with a `bg-violet-600` label pill; highlights = amber (`rgba(217,119,6,0.55)` / `rgba(251,191,36,0.08)`) with a `bg-amber-600` pill. The pill reads `<type> · <label>` (label truncated at 18 chars). Overlays are `z-[16] pointer-events-none` in Phase 1 (visual sync only — resize/move/select/delete arrive in later phases).


## 16. Demo Mode
- **Activation**: Set `DEMO_MODE=true` and `NEXT_PUBLIC_DEMO_MODE=true` as environment variables. Typically done via Docker Compose (see `docker-compose.yml`, the `livemaid-demo` service).
- **Read-Only Storage**: When `DEMO_MODE=true`, the server reads diagram data from the `./demo/` directory instead of `./data/`. The `saveDiagram` and `deleteDiagram` functions in `src/lib/api/storage.ts` are no-ops.
- **Disabled Write APIs**: The `POST /api/diagrams` (create) and `DELETE /api/diagrams/[id]` routes return HTTP 403. The `PUT /api/diagrams/[id]` route silently returns the existing document unchanged instead of saving.
- **Client-Side Guard**: `useEditorState` checks `NEXT_PUBLIC_DEMO_MODE` and skips the debounced auto-save entirely.
- **Demo Banner**: A persistent amber `DemoBanner` component (at `src/components/DemoBanner.tsx`) is rendered below the nav/header on both the Dashboard and the Editor. It clearly states that all changes are temporary and won't be saved.
- **Hidden Write Actions**: In demo mode, the "New Diagram" and "Create Diagram" buttons are hidden on the Dashboard. The rename and delete icon buttons on `DiagramCard` are also hidden. In the editor header, the "New Diagram", "Duplicate", and "Rename" menu items are removed, and the save status indicator shows "Read Only" instead of "Saved"/"Saving...".
- **Demo Content**: Populate the `./demo/` folder with pre-seeded `.json` diagram files (same format as `./data/`) that showcase the app's capabilities.

## 17. Editor Header & Chrome (`EditorHeader.tsx`)
- **Branding parity with the Dashboard**: The editor header shows the purple `LayoutTemplate` logo box and the `LiveMaid` wordmark (`text-xl font-semibold tracking-tight`) styled to match the Dashboard nav. The longer slogan was removed from the editor header (it stays on the Dashboard) to leave room for the breadcrumb.
- **Inline Diagram Rename (header breadcrumb)**: The diagram name in the breadcrumb is inline-editable. A `PencilLine` button appears next to the name on hover; clicking the name or the pencil swaps it for an `<input>` (pre-selected). **Enter** commits via `onRenameInline` (PUT `/api/diagrams/[id]`, optimistic `setDoc`, "Diagram renamed" toast); **Escape** cancels and restores. The legacy menu "Rename" dialog still exists and shares the same persistence path. Both are disabled in demo mode.
- **Export / History button parity**: The header `Export` and `History` buttons use identical chrome (`h-9`, `px-3`, `text-sm`, bordered) so they are pixel-matched.
- **Auto Number toggle (sequence top toolbar)**: In sequence diagrams, `LiveMaidEditor`'s top toolbar shows an `AUTO NUMBER` label (uppercase, letter-spaced, muted) next to a switch. Toggling it adds or removes the `autonumber` line right after `sequenceDiagram` via `handleCodeChange`, so undo/redo and re-render behave normally.

## 18. Inline Toolbar Reliability (Sequence / Edge / Node Manipulation Toolbars)
All three floating manipulation toolbars (`SequenceManipulationToolbar`, `EdgeManipulationToolbar`, `NodeManipulationToolbar`) are nested inside the selection box and marked with `data-inline-toolbar` + `data-scale-lock`, with a transparent padding shield and `stopPropagation` on their own pointer events. The following guards keep them robust against the canvas hit-testing underneath:
- **Hover suppression over the toolbar (both hover paths)**: A floating-UI guard — `document.elementsFromPoint(clientX, clientY).some(el => el.closest('[data-inline-toolbar]') || el.closest('[data-scale-lock]') || el.closest('[data-scale-lock-border]'))` — runs at the top of BOTH sequence hover paths (`handleSequenceHoverOver` for `onMouseOver`, and the RAF-throttled `_handleMouseMoveInner` for `onMouseMove`). When the cursor is over a toolbar it clears all sequence hover state and returns, so the hover grab overlay of the message *behind* the toolbar never renders on top of it (which previously stole the press and reselected the wrong message). Both paths need the guard because `onMouseMove` keeps firing while the cursor sits still over the toolbar.
- **Stacking order**: The selection-box border `<div>` that nests the toolbar is `z-[22]`, above the `z-[21]` sequence hover grab overlays, so the toolbar paints and hit-tests above neighbouring-message overlays.
- **Dropdown toggles fire on capture-phase mousedown**: The custom Message Type / Move dropdowns (plain `useState`, not Radix) toggle on `onMouseDownCapture`, not `onClick`. The toolbar container has a native bubble-phase `stopPropagation` listener (added via `addEventListener` in a `useEffect`) to block canvas leakage; React delegates synthetic handlers at the root, so a bubble-phase React `onPointerDown`/`onMouseDown` is killed by that native listener before it reaches the root. Capture-phase handlers fire root→target BEFORE any bubble listener, so the dropdown opens reliably even when a transient re-render moves the toolbar so the browser fires no native `click`. (A no-op `onClick={e => e.stopPropagation()}` remains to avoid a double-toggle.)
- **Never render the flowchart Node toolbar on a sequence diagram**: The toolbar ternary in `EditorCanvas` (`isEdgeId → Edge`, `SEQ_* → Sequence`, else `Node`) has a defensive `currentType === 'sequence' ? null :` branch before the `NodeManipulationToolbar` fallback. This prevents the wrong (flowchart) style bar from flashing during the brief window where `onDeselect` has cleared `selectedNodeId` (a separate state setter) while `selectionBox` still lingers — e.g. during a zoom/deselect transition (all zoom controls and `onZoomStart`/`onPinchStart` call `onDeselect`).
